version: '2.1'
services:
  postgres:
    extends:
      file: docker-compose.services.yml
      service: postgres10
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=Ahdae5ie
    volumes:
      - ./_state/postgres-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
